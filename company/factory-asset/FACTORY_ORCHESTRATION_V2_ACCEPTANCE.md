# Factory Orchestration v2 Synthetic Acceptance

Task: `FA-140`
Status: PASS
Date: 2026-09-04

The composed acceptance exercises the actual Factory v2 modules with zero live-provider/network calls.

Coverage:
- provider-original PNG -> intake -> synthetic bounded upscale -> derivatives/QA -> clean rights signal -> metadata/package -> human-friendly JPEG listing alias -> `WAITING_FOUNDER_QC`;
- provider-original JPEG follows the same path and proves format-agnostic intake;
- native procedural pattern produces actual editable SVG plus PNG preview through `PROCEDURAL_PATTERN_V0_1`; EPS remains explicitly uncertified;
- motion expression passes FA-130 temporal-value gate, routes through `MOTION_ENGINE_V0_1`, and is bound to the accepted real FA-043 MP4 master evidence;
- cognition covers zero-call fixed-Blueprint reuse and new-family Division01 AUTHOR + Executive CHALLENGE;
- conditional upscale includes transformed raster paths plus a sufficient-master `NOOP_SUFFICIENT` decision;
- FA-138 failure journal proves retryable crash/resume without lineage loss;
- fail-closed evidence aggregates unresolved rights review, package blockers, postproduction state violations and mislabeled/truncated/blank/frozen motion controls;
- Telegram milestone sets include `PRODUCTION_STARTED`, `ARTIFACT_CREATED`, `QA_QC_UPDATE`, and `WAITING_FOUNDER_QC`.

No binary IPTC/XMP injection is claimed in this acceptance. FA-141 owns that byte-mutating packaging feature and its read-back QA.