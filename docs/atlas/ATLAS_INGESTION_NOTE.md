# Atlas and Pipeline Canon Ingestion Note v1

Status: REVIEW NOTE
Date: 2026-08-24
Scope: provenance and exclusions for the docs-only canon synthesis

## Ingested

- Human-Centric Demand Atlas source: human-demand equation, object-first model,
  Intelligence Director discipline, Opportunity Score, platform isolation, and
  research-to-feedback loop.
- Digital Income Pipeline Map v0.1: the A–O holdings pillar taxonomy.
- Blueprint Batch-1 v2: M-001 primitive, event-driven scaling, Zero Trash
  routing, ERVA denominator, and active five-marketplace cohort.
- Founder directive: `$1B / 3Y` is governed through net profit and annualized
  run-rate, not gross revenue or valuation.

## Deliberately Excluded

- raw chat transcripts and motivational dialogue: high context cost, low runtime
  decision density;
- raw 187 KB source duplication: canon must be bounded and boot-safe;
- fixed `millions` or `billions` candidate claims: registries have not been
  enumerated and deduplicated;
- speculative royalty, sales-velocity, portfolio, or `$1B` scenario numbers:
  they are not executed receipts;
- historical platform-policy statements: each active surface requires a current
  official Platform Contract;
- the candidate marketplace CSV as policy truth: it is discovery input only;
- automatic `FAIL → social`: rights/safety failures cannot be repurposed and
  clean residuals still require transformation plus Pillar A authority.

## Supporting-Input Boundary

`SCENARIO_1B_QUANTITY_GAME.xlsx` (now at `docs/atlas/`) and
`MATRIX_6_PLATFORM_TOS_STRICTNESS.md` (now at `docs/pipeline/`) were named by
the Founder and were not available during the v1 synthesis; they are now
committed as **supporting inputs**, not canon authority:

- the scenario workbook is a gross-revenue planning proxy with assumed
  parameters (asset/day, $/asset/day, marketplace count, division count); its
  outputs are `HYPOTHESIS` until real receipts exist, are not a net-profit or
  run-rate forecast, and must never be cited as observed ERVA;
- the platform matrix treats Adobe ~10% versus others ~80% as `HYPOTHESIS`
  until measured against actual review receipts; its cohort is five independent
  marketplaces plus Magnific as a non-marketplace recovery lane. Per-platform
  rules remain governed by dated Platform Contracts.

The explicit Founder North Star decision (`$1B/3Y` = net profit + run-rate)
remains canonical. Platform strictness remains a hypothesis until current
contracts and real acceptance receipts exist.

## Result

The new canon is self-contained inside the repository and has no runtime
dependency on source-drive paths. It changes knowledge loaded at boot only. It
does not mutate runtime state or authorize production, publication, accounts,
spend, or cross-pillar activation.
